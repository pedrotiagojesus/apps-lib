// CSS
import "./Loading.css";

const Loading = () => {
    return (
        <div className="loading">
            <span className="loader">L &nbsp; ading</span>
        </div>
    );
};

export default Loading;
